allprojects {
}

subprojects {
    apply plugin: "java"
    apply plugin: "idea"

    repositories {
        mavenCentral()
    }

    dependencies {
        testCompile "junit:junit:4+"
        compile 'com.google.code.gson:gson:2.8.0'
    }
}
