def main = "edu.gvsu.cis.campbjos.central.CentralServer"

jar {
    baseName = 'p2p-server'
    manifest {
        attributes('Main-Class': main)
    }
}

dependencies {
    compile project(':common')
    compile 'org.apache.commons:commons-lang3:3.3.2'
}
